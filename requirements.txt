# Python 3.10. Pinned to the working `logitspec` stack (torch 2.7.1 + CUDA 11.8).
# Install:
#   conda create -n echo python=3.10 -y
#   conda activate echo
#   pip install -r requirements.txt
#   pip install -e ./echo/automaton

--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.7.1+cu118
transformers==4.57.1
accelerate==1.6.0
fschat==0.2.31
numpy==2.2.6
tqdm==4.67.1
huggingface-hub==0.36.0
tokenizers==0.22.2
safetensors==0.6.2
sentencepiece==0.2.0
protobuf==3.20.3
shortuuid==1.0.13
tabulate==0.9.0

# automaton build
Cython==3.2.4
setuptools>=65
wheel

# fschat 0.2.31 declares pydantic<2, but logitspec runs pydantic 2.12.5 +
# fastapi 0.120.3. Install fschat first, then overlay those two (see README).
rich==14.2.0
prompt_toolkit==3.0.52
tiktoken==0.12.0
aiohttp==3.13.2
httpx==0.28.1
markdown2==2.5.4
nh3==0.3.2
requests
# pulled in by fastchat.llm_judge.common (eval.py imports load_questions)
openai==0.28.0
anthropic==0.5.0
packaging==25.0
filelock==3.20.0
PyYAML==6.0.3
regex==2025.10.23
psutil==7.1.2
typing_extensions==4.15.0
anyio==3.7.1

# logitspec overlay (install after the rest; fschat metadata rejects pydantic 2)
# pydantic==2.12.5
# pydantic_core==2.41.5
# fastapi==0.120.3
# uvicorn==0.38.0
